// Tantangan koding codewars

// Reverse words

// 1. Cara bar bar

// function filter_list(l) {
//   return l.filter((list) => typeof list === "number");
// }

// 2. Cara simple

const filter_list = (l) => l.filter((list) => typeof list === "number");

console.log(filter_list([1, 4, 5, 2, "asdad", 2, "sad"]));
